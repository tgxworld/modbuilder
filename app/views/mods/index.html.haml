%h1 Modules

.row
  - @mods.each do |mod|
    .small-12.columns
      %ul.no-bullet
        %li
          = link_to mod_path(mod) do
            = image_tag mod.avatar, class: 'mod-image-thumb'
            = mod.name
            = mod.code
